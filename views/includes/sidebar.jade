.well.sidebar-nav
  ul.nav.nav-list
    li.nav-header #{__i('channel')}
    each ch in channels
      li(class=(typeof cid != 'undefined' && cid == ch.id) ? 'active':''): a(href='/ch/#{ch.id}') #{ch.title}


